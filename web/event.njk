---
layout: layouts/event
tags:
  - myEvents
pagination:
  alias: event
  data: events
  size: 1
  addAllPagesToCollections: true
permalink: events/{{ event.content.slug.current | slug }}/index.html
eleventyComputed:
  pageTitle: "{{ event.content.name }}"
renderData:
  title: "{{ event.content.name }}"
  metaDesc: "{{ event.content.shortDescription }}"
  socialImage: "{{ event.content.previewImage }}"
---
